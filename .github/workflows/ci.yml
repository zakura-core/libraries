name: CI

# The test and cross-target suites below are adapted from each upstream
# repository's primary CI workflow, as inspected on 2026-08-11. Book and API
# documentation jobs are omitted. Shared lint, formatting, and MSRV checks use
# the same policy as zakura-core/zakura.

on:
  # Allow maintainers to toggle the ci-backfill label when an unchanged PR
  # needs a fresh required test success check.
  pull_request:
    branches: [main, "feat/**", "release/**", "agent/**"]
    types: [opened, synchronize, reopened, labeled, unlabeled]
  push:
    branches:
      - main
  merge_group:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  changes:
    name: Detect changed upstreams
    runs-on: ubuntu-latest
    timeout-minutes: 60
    outputs:
      halo2: ${{ steps.filter.outputs.halo2 }}
      halo2_legacy_pdqsort: ${{ steps.filter.outputs.halo2_legacy_pdqsort }}
      orchard: ${{ steps.filter.outputs.orchard }}
      pasta_curves: ${{ steps.filter.outputs.pasta_curves }}
      reddsa: ${{ steps.filter.outputs.reddsa }}
      redjubjub: ${{ steps.filter.outputs.redjubjub }}
      sapling_crypto: ${{ steps.filter.outputs.sapling_crypto }}
      sinsemilla: ${{ steps.filter.outputs.sinsemilla }}
      librustzcash: ${{ steps.filter.outputs.librustzcash }}
      rust_checks: ${{ steps.filter.outputs.rust_checks }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Match changed paths to upstream repositories
        id: filter
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          PUSH_BASE_SHA: ${{ github.event.before }}
          PUSH_HEAD_SHA: ${{ github.sha }}
          MERGE_BASE_SHA: ${{ github.event.merge_group.base_sha }}
          MERGE_HEAD_SHA: ${{ github.event.merge_group.head_sha }}
        run: |
          case "$EVENT_NAME" in
            pull_request)
              base_sha=$PR_BASE_SHA
              head_sha=$PR_HEAD_SHA
              ;;
            push)
              base_sha=$PUSH_BASE_SHA
              head_sha=$PUSH_HEAD_SHA
              ;;
            merge_group)
              base_sha=$MERGE_BASE_SHA
              head_sha=$MERGE_HEAD_SHA
              ;;
            *)
              echo "Unsupported event: $EVENT_NAME" >&2
              exit 1
              ;;
          esac

          if [[ "$base_sha" == "0000000000000000000000000000000000000000" ]] ||
            ! git cat-file -e "${base_sha}^{commit}" 2>/dev/null; then
            # Diff a new ref, or an unreachable pre-force-push base, from the
            # empty tree. The root manifest then conservatively selects all suites.
            base_sha=$(git hash-object -t tree /dev/null)
          elif [[ "$EVENT_NAME" == pull_request ]]; then
            base_sha=$(git merge-base "$base_sha" "$head_sha")
          fi
          changed=$(git diff --name-only --no-renames "$base_sha" "$head_sha")

          halo2=false
          halo2_legacy_pdqsort=false
          orchard=false
          pasta_curves=false
          reddsa=false
          redjubjub=false
          sapling_crypto=false
          sinsemilla=false
          librustzcash=false
          shared=false

          # Trip on workspace membership drift so maintainers revisit this
          # array, the path-routing cases, and the reverse-dependency graph
          # together. This does not prove those three mappings are equivalent.
          handled_members=(
            bellman
            bls12_381
            halo2_gadgets
            halo2_legacy_pdqsort
            halo2_poseidon
            halo2_proofs
            jubjub
            orchard
            pairing
            pasta_curves
            reddsa
            redjubjub
            sapling-crypto
            sinsemilla
            zcash_keys
            zcash_primitives
            zcash_proofs
          )
          workspace_packages=$(python3 .github/scripts/workspace_packages.py)
          workspace_members=()
          while IFS= read -r member; do
            workspace_members+=("$member")
          done < <(jq -r '.[].directory' <<< "$workspace_packages")
          if ! diff -u \
            <(printf '%s\n' "${handled_members[@]}" | sort) \
            <(printf '%s\n' "${workspace_members[@]}" | sort); then
            echo "Workspace membership changed; update all CI routing mappings" >&2
            exit 1
          fi

          while IFS= read -r path; do
            case "$path" in
              halo2_gadgets/*|halo2_poseidon/*|halo2_proofs/*)
                halo2=true
                ;;
              halo2_legacy_pdqsort/*)
                halo2_legacy_pdqsort=true
                ;;
              orchard/*)
                orchard=true
                ;;
              pasta_curves/*)
                pasta_curves=true
                ;;
              reddsa/*)
                reddsa=true
                ;;
              redjubjub/*)
                redjubjub=true
                ;;
              bellman/*|bls12_381/*|jubjub/*|pairing/*)
                # These crates form the shared Sapling/Groth16 foundation.
                # Jubjub also feeds RedDSA, so conservatively run both stacks
                # for changes to any member of this tightly-coupled group.
                reddsa=true
                sapling_crypto=true
                ;;
              sapling-crypto/*)
                sapling_crypto=true
                ;;
              sinsemilla/*)
                sinsemilla=true
                ;;
              zcash_keys/*|zcash_primitives/*|zcash_proofs/*)
                librustzcash=true
                ;;
              Cargo.toml|Cargo.lock|rust-toolchain.toml|.cargo/*|.github/scripts/check_rust_group.sh|.github/scripts/workspace_packages.py|.github/workflows/ci.yml|.github/workflows/maintenance.yml)
                shared=true
                ;;
            esac
          done <<< "$changed"

          if [[ "$shared" == true ]]; then
            halo2=true
            halo2_legacy_pdqsort=true
            orchard=true
            pasta_curves=true
            reddsa=true
            redjubjub=true
            sapling_crypto=true
            sinsemilla=true
            librustzcash=true
          fi

          # Expand changed packages through the workspace's reverse-dependency
          # graph. Keep these assignments ordered from foundational crates to
          # their downstream consumers so each transitive edge is applied. The
          # member check above catches new crates; update this graph when a
          # workspace path-dependency edge changes.
          if [[ "$pasta_curves" == true ]]; then
            halo2=true
            reddsa=true
            sinsemilla=true
          fi
          if [[ "$halo2_legacy_pdqsort" == true ]]; then
            halo2=true
          fi
          if [[ "$sinsemilla" == true ]]; then
            halo2=true
            orchard=true
          fi
          if [[ "$halo2" == true ]]; then
            orchard=true
          fi
          if [[ "$reddsa" == true ]]; then
            redjubjub=true
            orchard=true
          fi
          if [[ "$redjubjub" == true ]]; then
            sapling_crypto=true
          fi
          if [[ "$orchard" == true ]]; then
            librustzcash=true
          fi
          if [[ "$sapling_crypto" == true ]]; then
            librustzcash=true
          fi

          rust_checks=false
          if [[ "$halo2" == true ||
            "$halo2_legacy_pdqsort" == true ||
            "$orchard" == true ||
            "$pasta_curves" == true ||
            "$reddsa" == true ||
            "$redjubjub" == true ||
            "$sapling_crypto" == true ||
            "$sinsemilla" == true ||
            "$librustzcash" == true ]]; then
            rust_checks=true
          fi

          {
            echo "halo2=$halo2"
            echo "halo2_legacy_pdqsort=$halo2_legacy_pdqsort"
            echo "orchard=$orchard"
            echo "pasta_curves=$pasta_curves"
            echo "reddsa=$reddsa"
            echo "redjubjub=$redjubjub"
            echo "sapling_crypto=$sapling_crypto"
            echo "sinsemilla=$sinsemilla"
            echo "librustzcash=$librustzcash"
            echo "rust_checks=$rust_checks"
          } >> "$GITHUB_OUTPUT"

  rust-checks:
    name: Rust checks
    needs: changes
    if: needs.changes.outputs.rust_checks == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable
        with:
          components: clippy
      - uses: dtolnay/rust-toolchain@2eae45db285e407f22119950686d47e1101e071b # 1.88.0
      - name: Install build dependencies
        run: sudo apt-get update && sudo apt-get install -y libfontconfig1-dev
      # One package per invocation: a grouped invocation lets Cargo unify
      # features across the group, masking a package that cannot build
      # against its own manifest alone.
      - name: halo2 / Stable and MSRV checks
        if: ${{ !cancelled() && needs.changes.outputs.halo2 == 'true' }}
        run: |
          bash .github/scripts/check_rust_group.sh zakura-halo2-gadgets false false
          bash .github/scripts/check_rust_group.sh zakura-halo2-poseidon false false
          bash .github/scripts/check_rust_group.sh zakura-halo2-proofs false false
      - name: halo2_legacy_pdqsort / Stable and MSRV checks
        if: ${{ !cancelled() && needs.changes.outputs.halo2_legacy_pdqsort == 'true' }}
        run: >-
          bash .github/scripts/check_rust_group.sh
          zakura-halo2-legacy-pdqsort false true
      - name: orchard / Stable and MSRV checks
        if: ${{ !cancelled() && needs.changes.outputs.orchard == 'true' }}
        run: bash .github/scripts/check_rust_group.sh zakura-orchard true false
      - name: pasta_curves / Stable and MSRV checks
        if: ${{ !cancelled() && needs.changes.outputs.pasta_curves == 'true' }}
        run: bash .github/scripts/check_rust_group.sh zakura-pasta-curves false false
      - name: reddsa / Stable and MSRV checks
        if: ${{ !cancelled() && needs.changes.outputs.reddsa == 'true' }}
        run: bash .github/scripts/check_rust_group.sh zakura-reddsa false false
      - name: redjubjub / Stable and MSRV checks
        if: ${{ !cancelled() && needs.changes.outputs.redjubjub == 'true' }}
        run: bash .github/scripts/check_rust_group.sh zakura-redjubjub false false
      - name: Sapling foundation / Stable and MSRV checks
        if: ${{ !cancelled() && needs.changes.outputs.sapling_crypto == 'true' }}
        run: >-
          bash .github/scripts/check_rust_group.sh
          "zakura-pairing zakura-bls12-381 zakura-jubjub zakura-bellman"
          false true
      - name: BLS12-381 / Experimental feature check
        if: ${{ !cancelled() && needs.changes.outputs.sapling_crypto == 'true' }}
        run: >-
          cargo +1.88.0 check --locked -p zakura-bls12-381
          --no-default-features --features experimental
      - name: sapling-crypto / Stable and MSRV checks
        if: ${{ !cancelled() && needs.changes.outputs.sapling_crypto == 'true' }}
        run: bash .github/scripts/check_rust_group.sh zakura-sapling-crypto true false
      - name: sinsemilla / Stable and MSRV checks
        if: ${{ !cancelled() && needs.changes.outputs.sinsemilla == 'true' }}
        run: bash .github/scripts/check_rust_group.sh zakura-sinsemilla false true
      - name: librustzcash / Stable and MSRV checks
        if: ${{ !cancelled() && needs.changes.outputs.librustzcash == 'true' }}
        run: >-
          bash .github/scripts/check_rust_group.sh
          "zakura-keys zakura-primitives zakura-proofs" true false

  formatting:
    name: Formatting
    needs: changes
    if: needs.changes.outputs.rust_checks == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@7c8d7d138f5c09cef361f8214cf96882cd029cdb # nightly
        with:
          components: rustfmt
      - name: Check formatting
        run: cargo +nightly fmt --all -- --check

  # zcash/halo2 .github/workflows/ci.yml @
  # 723a90bc598d986bdf59941a8011c60a0819d6b9
  halo2-test:
    name: halo2 / Test on Linux${{ matrix.name_suffix }}
    needs: changes
    if: needs.changes.outputs.halo2 == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        stage: [stable, nightly]
        include:
          - stage: stable
            features: ""
            name_suffix: ""
          - stage: nightly
            features: nightly
            name_suffix: " with nightly features"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable
      - name: Run tests
        shell: bash
        env:
          STAGE_FEATURE: ${{ matrix.features }}
        run: |
          features=batch,dev-graph,gadget-traces,test-dependencies
          if [[ -n "$STAGE_FEATURE" ]]; then
            features="$features,$STAGE_FEATURE"
          fi
          cargo +stable test --verbose --release \
            -p zakura-halo2-gadgets \
            -p zakura-halo2-poseidon \
            -p zakura-halo2-proofs \
            --no-default-features \
            --features "$features"
      - name: Verify working directory is clean
        run: git diff --exit-code

  halo2-platform-smoke:
    name: halo2 / Platform smoke test on ${{ matrix.os }}
    needs: changes
    if: needs.changes.outputs.halo2 == 'true'
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macOS-latest]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable
      - name: Compile tests
        run: >-
          cargo +stable test --verbose --release
          -p zakura-halo2-gadgets
          -p zakura-halo2-poseidon
          -p zakura-halo2-proofs
          --no-default-features
          --features batch,dev-graph,gadget-traces,test-dependencies
          --no-run
      - name: Test platform randomness and polynomial arithmetic
        run: >-
          cargo +stable test --verbose --release
          -p zakura-halo2-gadgets
          -p zakura-halo2-poseidon
          -p zakura-halo2-proofs
          --no-default-features
          --features batch,dev-graph,gadget-traces,test-dependencies
          poly::domain::test_rotate
          -- --exact
      - name: Verify working directory is clean
        run: git diff --exit-code

  halo2-test-32-bit:
    name: halo2 / Test i686${{ matrix.name_suffix }}
    needs: changes
    if: needs.changes.outputs.halo2 == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        stage: [stable, nightly]
        include:
          - stage: stable
            name_suffix: ""
          - stage: nightly
            name_suffix: " with nightly features"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@2eae45db285e407f22119950686d47e1101e071b # 1.88.0
        with:
          targets: i686-unknown-linux-gnu
      - name: Install cross-platform support dependencies
        run: sudo apt-get update && sudo apt-get install -y gcc-multilib
      - name: Run tests
        shell: bash
        env:
          STAGE: ${{ matrix.stage }}
        run: |
          features=batch,dev-graph,gadget-traces,test-dependencies
          if [[ "$STAGE" != stable ]]; then
            features="$features,$STAGE"
          fi
          # This job is a 32-bit portability gate, not a full regression
          # suite: it exists to exercise pointer-width-dependent code
          # (PrimeFieldBits' u32 limbs, usize assumptions, u128 lowering)
          # and proof (de)serialization on a 32-bit target. The skipped
          # tests below are the handful of multi-minute MockProver /
          # constant-generation tests; they re-run arithmetic that the
          # remaining tests already cover and account for ~90% of this
          # job's runtime (the full suite runs on x86_64 above). All
          # stored-proof verifier tests, the real prove-and-verify path
          # (halo2_proofs plonk tests), and one MockProver satisfaction
          # test per chip still run here. Note --skip matches substrings,
          # so ecc_chip and invalid_magnitude_sign also cover their
          # _4_5b variants.
          cargo +1.88.0 test --verbose --release \
            -p zakura-halo2-gadgets \
            -p zakura-halo2-poseidon \
            -p zakura-halo2-proofs \
            --target i686-unknown-linux-gnu \
            --no-default-features \
            --features "$features" \
            -- \
            --skip ecc::chip::constants::tests::lagrange_coeffs \
            --skip ecc::chip::constants::tests::zs_and_us \
            --skip ecc::chip::mul_fixed::short::tests::invalid_magnitude_sign \
            --skip ecc::tests::ecc_chip \
            --skip sinsemilla::tests::sinsemilla_chip \
            --skip utilities::lookup_range_check::tests::short_range_check

  halo2-build:
    name: halo2 / Build ${{ matrix.target }}
    needs: changes
    if: needs.changes.outputs.halo2 == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        target:
          - wasm32-wasip1
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@2eae45db285e407f22119950686d47e1101e071b # 1.88.0
        with:
          targets: ${{ matrix.target }}
      - name: Build target
        run: >-
          cargo +1.88.0 build
          -p zakura-halo2-gadgets
          -p zakura-halo2-poseidon
          -p zakura-halo2-proofs
          --target ${{ matrix.target }}
          --no-default-features
          --features batch,dev-graph,gadget-traces,nightly

  # zcash/orchard .github/workflows/ci.yml @
  # 3f7ebf716a04bb76f35afe9fa5296064fcd451ca
  orchard-test:
    name: orchard / Test on Linux
    needs: changes
    if: needs.changes.outputs.orchard == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable
      - name: Run tests
        run: cargo +stable test -p zakura-orchard --verbose
      - name: Run weighted-merkle library tests
        # The weighted-merkle feature is not in the crate defaults, so its
        # gated tests (e.g. the weighted/unweighted Merkle equivalence
        # proptest in tree.rs) never execute under the default-feature run
        # above. They live in the library target, so --lib re-runs only the
        # cheap lib suite with the feature enabled rather than repeating the
        # integration tests.
        run: >-
          cargo +stable test -p zakura-orchard
          --features weighted-merkle
          --lib
          --verbose
      - name: Compile-check verifier-fingerprint capture
        run: >-
          cargo +stable test -p zakura-orchard
          --features verifier-fingerprint
          --no-run
          --verbose
      - name: Verify working directory is clean
        run: git diff --exit-code

  sinsemilla-weighted-test:
    name: sinsemilla / Test weighted Merkle evaluator on Linux
    needs: changes
    if: needs.changes.outputs.sinsemilla == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable
      - name: Run weighted Merkle evaluator tests
        run: >-
          cargo +stable test -p zakura-sinsemilla
          --lib
          --verbose
          -- weighted::tests

  orchard-platform-smoke:
    name: orchard / Platform smoke test on ${{ matrix.os }}
    needs: changes
    if: needs.changes.outputs.orchard == 'true'
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macOS-latest]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable
      - name: Compile tests
        run: cargo +stable test -p zakura-orchard --verbose --no-run
      - name: Create and verify a proof individually and in a batch
        run: cargo +stable test -p zakura-orchard --verbose --test platform_smoke
      - name: Run weighted Merkle vector tests on the Apple AArch64 assembly backend
        # The macOS runner is Apple silicon, the only place the
        # `pasta_curves/aarch64-asm` field backend is live. Linux already runs
        # the weighted-merkle lib tests on the portable backend; this re-runs
        # the Merkle fixed-vector tests (tree::tests) with the weighted
        # evaluator on top of the assembly field arithmetic, so both are
        # pinned to the same external bytes on real hardware.
        if: matrix.os == 'macOS-latest'
        run: >-
          cargo +stable test -p zakura-orchard
          --features weighted-merkle,pasta_curves/aarch64-asm
          --lib
          --verbose
          -- tree::tests
      - name: Verify working directory is clean
        run: git diff --exit-code

  orchard-no-std:
    name: orchard / Build no_std for ${{ matrix.target }}
    needs: changes
    if: needs.changes.outputs.orchard == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        target:
          - wasm32-wasip1
          - thumbv7em-none-eabihf
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@2eae45db285e407f22119950686d47e1101e071b # 1.88.0
        with:
          targets: ${{ matrix.target }}
      - name: Build no_std target
        run: >-
          cargo +1.88.0 build -p zakura-orchard
          --no-default-features
          --target ${{ matrix.target }}
          --verbose

  # zcash/pasta_curves .github/workflows/ci.yml @
  # bd896a571d34bceb3451a048a08cf21f78ef1942
  pasta-test:
    name: pasta_curves / Test on Linux (${{ matrix.features }})
    needs: changes
    if: needs.changes.outputs.pasta_curves == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        features: [--all-features, --no-default-features]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable
      - name: Run tests
        run: >-
          cargo +stable test -p zakura-pasta-curves
          --verbose
          --release
          ${{ matrix.features }}
      - name: Verify working directory is clean
        run: git diff --exit-code

  pasta-platform-smoke:
    name: pasta_curves / Platform smoke test on ${{ matrix.os }}
    needs: changes
    if: needs.changes.outputs.pasta_curves == 'true'
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macOS-latest]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable
      - name: Compile tests
        run: >-
          cargo +stable test -p zakura-pasta-curves
          --verbose
          --release
          --all-features
          --no-run
      - name: Test field arithmetic
        run: >-
          cargo +stable test -p zakura-pasta-curves
          --verbose
          --release
          --all-features
          test_inv
      - name: Test Apple AArch64 assembly
        if: runner.os == 'macOS' && runner.arch == 'ARM64'
        run: >-
          cargo +stable test -p zakura-pasta-curves
          --verbose
          --release
          --all-features
          aarch64_asm_matches_portable_arithmetic
      - name: Verify working directory is clean
        run: git diff --exit-code

  pasta-test-32-bit:
    name: pasta_curves / Test i686 (${{ matrix.features }})
    needs: changes
    if: needs.changes.outputs.pasta_curves == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        features: [--all-features, --no-default-features]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@2eae45db285e407f22119950686d47e1101e071b # 1.88.0
        with:
          targets: i686-unknown-linux-gnu
      - name: Install cross-platform support dependencies
        run: sudo apt-get update && sudo apt-get install -y gcc-multilib
      - name: Run tests
        run: >-
          cargo +1.88.0 test -p zakura-pasta-curves
          --verbose
          --target i686-unknown-linux-gnu
          ${{ matrix.features }}
      - name: Verify working directory is clean
        run: git diff --exit-code

  pasta-no-std:
    name: pasta_curves / Build no_std for ${{ matrix.target }}
    needs: changes
    if: needs.changes.outputs.pasta_curves == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        target:
          - thumbv6m-none-eabi
          - wasm32-unknown-unknown
          - wasm32-wasip1
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@2eae45db285e407f22119950686d47e1101e071b # 1.88.0
        with:
          targets: ${{ matrix.target }}
      - name: Build target
        run: >-
          cargo +1.88.0 build -p zakura-pasta-curves
          --verbose
          --target ${{ matrix.target }}
          --no-default-features

  # ZcashFoundation/reddsa .github/workflows/main.yml @
  # ad34fbe2eb3c3471f43823407b89f313745d51da
  reddsa-test:
    name: reddsa / Test all features
    needs: changes
    if: needs.changes.outputs.reddsa == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable
      - run: cargo +stable test -p zakura-reddsa --all-features --locked

  reddsa-no-std:
    name: reddsa / Build no_std
    needs: changes
    if: needs.changes.outputs.reddsa == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@2eae45db285e407f22119950686d47e1101e071b # 1.88.0
        with:
          targets: thumbv6m-none-eabi
      - run: >-
          cargo +1.88.0 build -p zakura-reddsa
          --no-default-features
          --target thumbv6m-none-eabi

  # ZcashFoundation/redjubjub .github/workflows/main.yml @
  # 5a299f0f2e7e3e7a478f3466f85d77ecbf4db6c6
  redjubjub-test:
    name: redjubjub / Test
    needs: changes
    if: needs.changes.outputs.redjubjub == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable
      - run: cargo +stable test -p zakura-redjubjub --all-features

  redjubjub-no-std:
    name: redjubjub / Build no_std for ${{ matrix.target }}
    needs: changes
    if: needs.changes.outputs.redjubjub == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        target:
          - wasm32-wasip1
          - thumbv7em-none-eabihf
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@2eae45db285e407f22119950686d47e1101e071b # 1.88.0
        with:
          targets: ${{ matrix.target }}
      - run: >-
          cargo +1.88.0 build -p zakura-redjubjub
          --no-default-features
          --target ${{ matrix.target }}
          --verbose

  # zcash/sapling-crypto .github/workflows/ci.yml @
  # 1b24fd9fd20284fdac1c99e0069dbda24e608e57
  sapling-test:
    name: Sapling stack / Test on Linux
    needs: changes
    if: needs.changes.outputs.sapling_crypto == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable
      - name: Run tests
        run: >-
          cargo +stable test
          -p zakura-pairing
          -p zakura-bls12-381
          -p zakura-jubjub
          -p zakura-bellman
          -p zakura-sapling-crypto
          --all-features
          --verbose
          --release
      - name: Run default-feature library tests
        # fused-pedersen is not in the crate defaults, so the --all-features
        # run above never exercises the original 8-bit exp-window tables.
        # They live in the library target, so --lib re-runs only the cheap
        # lib suite without the feature rather than repeating the whole stack.
        run: >-
          cargo +stable test -p zakura-sapling-crypto
          --lib
          --verbose
          --release
      - name: Verify working directory is clean
        run: git diff --exit-code

  sapling-platform-smoke:
    name: Sapling stack / Platform smoke test on ${{ matrix.os }}
    needs: changes
    if: needs.changes.outputs.sapling_crypto == 'true'
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macOS-latest]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable
      - name: Compile tests
        run: >-
          cargo +stable test
          -p zakura-pairing
          -p zakura-bls12-381
          -p zakura-jubjub
          -p zakura-bellman
          -p zakura-sapling-crypto
          --all-features
          --verbose
          --release
          --no-run
      - name: Test platform randomness and note encryption
        run: >-
          cargo +stable test -p zakura-sapling-crypto
          --all-features
          --verbose
          --release
          note_encryption::tests::decryption_with_invalid_tag
          -- --exact
      - name: Verify working directory is clean
        run: git diff --exit-code

  sapling-no-std:
    name: Sapling stack / Build no_std for ${{ matrix.target }}
    needs: changes
    if: needs.changes.outputs.sapling_crypto == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        target:
          - wasm32-wasip1
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@2eae45db285e407f22119950686d47e1101e071b # 1.88.0
        with:
          targets: ${{ matrix.target }}
      - name: Build target
        run: >-
          cargo +1.88.0 build
          -p zakura-pairing
          -p zakura-bls12-381
          -p zakura-jubjub
          -p zakura-bellman
          -p zakura-sapling-crypto
          --no-default-features
          --target ${{ matrix.target }}
          --verbose

  # zcash/librustzcash .github/workflows/ci.yml @
  # 82fa052a06e3af0058a73f320538f9b93193439c
  librustzcash-test:
    name: librustzcash subset / Test all features
    needs: changes
    if: needs.changes.outputs.librustzcash == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      CARGO_PROFILE_TEST_DEBUG: 1
      RAYON_NUM_THREADS: 2
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@2eae45db285e407f22119950686d47e1101e071b # 1.88.0
      - name: Run tests
        run: >-
          cargo +1.88.0 test
          -p zakura-keys
          -p zakura-primitives
          -p zakura-proofs
          --all-features
          -- --test-threads=2
      - name: Verify working directory is clean
        run: git diff --exit-code

  librustzcash-configurations:
    name: librustzcash subset / Configuration checks
    needs: changes
    if: needs.changes.outputs.librustzcash == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@2eae45db285e407f22119950686d47e1101e071b # 1.88.0
        with:
          components: clippy
      - name: Check no-default-features
        run: >-
          cargo +1.88.0 check
          -p zakura-keys
          -p zakura-primitives
          -p zakura-proofs
          --all-targets
          --no-default-features
      - name: Check transparent-inputs-only
        if: ${{ !cancelled() }}
        run: >-
          cargo +1.88.0 check
          -p zakura-keys
          -p zakura-primitives
          -p zakura-proofs
          --all-targets
          --no-default-features
          --features zakura-keys/transparent-inputs,zakura-primitives/transparent-inputs
      - name: Check transparent-key-encoding-only
        if: ${{ !cancelled() }}
        run: >-
          cargo +1.88.0 check
          -p zakura-keys
          -p zakura-primitives
          -p zakura-proofs
          --all-targets
          --no-default-features
          --features zakura-keys/transparent-key-encoding
      - name: Check Sapling-only
        if: ${{ !cancelled() }}
        run: >-
          cargo +1.88.0 check
          -p zakura-keys
          -p zakura-primitives
          -p zakura-proofs
          --all-targets
          --no-default-features
          --features zakura-keys/sapling,zakura-primitives/circuits
      - name: Check Orchard
        if: ${{ !cancelled() }}
        run: >-
          cargo +1.88.0 check
          -p zakura-keys
          -p zakura-primitives
          -p zakura-proofs
          --all-targets
          --no-default-features
          --features zakura-keys/orchard,zakura-primitives/circuits
      - name: Check all-features
        if: ${{ !cancelled() }}
        run: >-
          cargo +1.88.0 check
          -p zakura-keys
          -p zakura-primitives
          -p zakura-proofs
          --all-targets
          --all-features
      - name: Clippy each feature of each imported crate
        if: ${{ !cancelled() }}
        shell: bash
        run: |
          for package in zakura-keys zakura-primitives zakura-proofs; do
            cargo +1.88.0 metadata --no-deps --format-version 1 |
              jq -r --arg package "$package" \
                '.packages[] | select(.name == $package) | .features | keys[]' |
              while IFS= read -r feature; do
                cargo +1.88.0 clippy -p "$package" \
                  --no-deps \
                  --no-default-features \
                  --features "$feature"
              done
          done
      - name: Install beta Clippy
        if: ${{ !cancelled() }}
        continue-on-error: true
        uses: dtolnay/rust-toolchain@482f49bd06e5fc9083e4fd71e1327793b1c839fe # beta
        with:
          components: clippy
      - name: Clippy on beta
        if: ${{ !cancelled() }}
        continue-on-error: true
        run: >-
          cargo +beta clippy
          -p zakura-keys
          -p zakura-primitives
          -p zakura-proofs
          --no-deps
          --all-features
          --all-targets
          -- -W clippy::all

  librustzcash-no-std:
    name: librustzcash subset / Build ${{ matrix.target }}
    needs: changes
    if: needs.changes.outputs.librustzcash == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: wasm32-wasip1
            packages: -p zakura-proofs
            build-deps: gcc-multilib
          - target: thumbv7em-none-eabihf
            packages: -p zakura-keys -p zakura-primitives
            build-deps: gcc-arm-none-eabi
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@2eae45db285e407f22119950686d47e1101e071b # 1.88.0
        with:
          targets: ${{ matrix.target }}
      - name: Install cross-platform support dependencies
        if: matrix.build-deps != ''
        run: sudo apt-get update && sudo apt-get install -y ${{ matrix.build-deps }}
      - name: Build target
        shell: bash
        env:
          PACKAGES: ${{ matrix.packages }}
          TARGET: ${{ matrix.target }}
        run: cargo +1.88.0 build $PACKAGES --no-default-features --target "$TARGET" --verbose

  test-success:
    name: test success
    if: always()
    needs:
      - changes
      - rust-checks
      - formatting
      - halo2-test
      - halo2-platform-smoke
      - halo2-test-32-bit
      - halo2-build
      - orchard-test
      - orchard-platform-smoke
      - orchard-no-std
      - pasta-test
      - pasta-platform-smoke
      - pasta-test-32-bit
      - pasta-no-std
      - reddsa-test
      - reddsa-no-std
      - redjubjub-test
      - redjubjub-no-std
      - sapling-test
      - sapling-platform-smoke
      - sapling-no-std
      - librustzcash-test
      - librustzcash-configurations
      - librustzcash-no-std
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Decide whether the needed jobs succeeded or failed
        uses: re-actors/alls-green@05ac9388f0aebcb5727afa17fcccfecd6f8ec5fe # v1.2.2
        with:
          jobs: ${{ toJSON(needs) }}
          # Every job except the detector is path-gated. A skipped job is valid,
          # but a failed or cancelled job must still fail this aggregate status.
          allowed-skips: >-
            rust-checks,
            formatting,
            halo2-test,
            halo2-platform-smoke,
            halo2-test-32-bit,
            halo2-build,
            orchard-test,
            orchard-platform-smoke,
            orchard-no-std,
            pasta-test,
            pasta-platform-smoke,
            pasta-test-32-bit,
            pasta-no-std,
            reddsa-test,
            reddsa-no-std,
            redjubjub-test,
            redjubjub-no-std,
            sapling-test,
            sapling-platform-smoke,
            sapling-no-std,
            librustzcash-test,
            librustzcash-configurations,
            librustzcash-no-std
